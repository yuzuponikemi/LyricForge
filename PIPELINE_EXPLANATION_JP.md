# LyricForge パイプライン詳細解説

本ドキュメントでは、LyricForgeのパイプライン処理について、各工程の技術的背景、パラメータ調整、中間生成物、および動作確認方法を深く掘り下げて解説します。

---

## 1. Download ステージ (動画・音声取得)

**目的**: 分析対象となる高品質なメディア素材を確保する。

### 仕組み
`yt-dlp` ラッパーを使用し、ストリーミングサイトから「最高画質の動画」と「最高音質の音声」を個別に取得します。通常、ストリーミング配信では映像と音声が別々のストリームとして配信されているため、それらを劣化なしでそのまま取得（Direct Stream Copy）し、後処理で結合や変換を行います。

### 主なパラメータ (`config/settings.yaml` -> `downloader`)
*   `format`: ダウンロードする品質を指定。デフォルトは `bestvideo+bestaudio` で、最高画質と最高音質を狙います。
*   `audio_format`: 音声抽出時のフォーマット（デフォルト: `wav`）。非圧縮のWAV形式にすることで、後続の分離・認識処理での劣化を防ぎます。
*   `audio_quality`: 音声品質（`0`は最高品質）。

### 中間生成ファイル
*   `data/raw/{video_id}.mp4`: 元動画ファイル
*   `data/raw/{video_id}.wav`: 元音声ファイル（WAV形式）
*   `data/raw/{video_id}.info.json`: 動画のメタデータ（タイトル、投稿者、タグなど）

### 動作確認・検証
*   **確認方法**: `data/raw/` フォルダに生成された `.wav` ファイルを再生し、音声が正常に聞こえるか確認します。
*   **ログ**: `Video downloaded` および `Audio extracted` というログが出力されていれば成功です。

---

## 2. Separate ステージ (音源分離)

**目的**: 伴奏（インストゥルメンタル）と歌声（ボーカル）を分離し、文字起こしの精度を劇的に向上させる。

### 仕組み (なぜ分離できるのか？)
**Demucs (Hybrid Transformer Demucs)** モデルを使用します。これは、音声の波形データとスペクトログラム（周波数成分）の両方を解析する深層学習モデルです。
*   **U-Net構造**: 画像処理で有名なU-Netに似た構造を持ち、音声をエンコード（圧縮）して特徴を抽出し、デコード（復元）する過程で特定の楽器（ここではボーカル）以外の成分を除去します。
*   **ハイブリッドアプローチ**: 時間領域（波形）と周波数領域（スペクトル）の両方で処理を行うため、ドラムのアタック音のような急激な変化と、歌声のような持続的な音の両方を高精度に分離できます。

### 主なパラメータ (`separator`)
*   `model`: 使用するモデル（デフォルト: `htdemucs`）。`htdemucs_ft`（fine-tuned）にすると精度が上がる場合がありますが、処理時間が延びます。
*   `shifts`: 推論時に音声をランダムに少しずらして複数回処理し、その平均を取る回数（デフォルト: `1`）。値を増やすと（例: 2〜5）、分離ノイズが減り品質が向上しますが、計算時間は倍増します。
*   `overlap`: 分割処理する際の重複区間の割合（デフォルト: `0.25`）。つなぎ目のノイズを低減します。

### 中間生成ファイル
*   `data/stems/{video_id}/vocals.wav`: 抽出されたボーカルトラック
*   `data/stems/{video_id}/no_vocals.wav`: ボーカルを取り除いたインストゥルメンタル（カラオケ音源）

### 動作確認・検証
*   **確認方法**: `vocals.wav` を再生し、伴奏がほぼ消え、歌声だけが聞こえるか確認します。逆に `no_vocals.wav` はカラオケ音源として成立しているか確認します。
*   **ログ**: `Audio separation completed` と表示され、出力パスが記録されます。

---

## 3. Transcribe ステージ (音声認識・文字起こし)

**目的**: 歌声から歌詞テキストと、その発話タイミング（タイムスタンプ）を抽出する。

### 仕組み (なぜモデルはワークするのか？)
**Faster-Whisper (Whisper)** モデルを使用します。
*   **Transformerアーキテクチャ**: 大量の音声データで学習されたTransformerモデルが、音声スペクトログラムをトークン（単語や文字の断片）の列に変換します。
*   **Attentionメカニズム**: 「今、音声のどこに注目すべきか」を自動的に判断しながらテキストを生成するため、長いフレーズでも文脈を考慮した認識が可能です。
*   **VAD (Voice Activity Detection)**: 音声区間検出フィルタを併用し、ブレスや無音区間での幻覚（ハルシネーション：存在しない言葉を生成してしまう現象）を抑制します。

### 主なパラメータ (`transcriber`)
*   `model`: モデルサイズ（`large-v3`, `medium`, `small`など）。`large-v3` は最も高精度ですが、GPUメモリ（VRAM）を約10GB消費します。
*   `beam_size`: 探索の幅（デフォルト: `5`）。複数の候補を並行して探索し、最も確からしい文を選びます。
*   `vad_filter`: `true` にするとVADフィルタが有効になります。歌詞のない間奏部分での誤認識を防ぐのに効果的です。
*   `compute_type`: `float16` または `int8`。`int8` にすると精度をほぼ維持したままメモリ消費量を削減できます。

### 中間生成ファイル
*   `data/output/{video_id}_raw.json`: Whisperが出力した生の文字起こしデータ。
    *   構造: `segments` リストの中に `start`, `end`, `text` および単語ごとの `words` リストが含まれます。

### 動作確認・検証
*   **確認方法**: 生成されたJSONファイルを開き、`text` フィールドが歌の内容と一致しているか確認します。
*   **ログ**: `Transcription completed` と表示され、検出された言語やセグメント数が出力されます。

---

## 4. Refine ステージ (LLMによる補正)

**目的**: 音声認識の誤りを正し、人間が読みやすい「歌詞」の形式に整える。

### 仕組み
ローカルLLM (**Ollama** 経由の Llama 3 など) を使用し、Whisperの出力を「校正」します。
*   **コンテキスト理解**: LLMは文脈を理解できるため、「海」を「膿」と誤変換しているような同音異義語のミスや、歌詞として不自然な言い回しを修正できます。
*   **フォーマット維持**: プロンプトエンジニアリングにより、テキストの内容だけを修正し、行数や順序を維持させることで、Whisperが取得したタイムスタンプとの対応関係を壊さないように制御しています。

### 主なパラメータ (`refiner`)
*   `model`: 使用するLLM（例: `llama3.2:latest`）。モデルの賢さが補正精度に直結します。
*   `prompt_template`: LLMへの指示書。「文字起こしの誤りを直して」「タイムスタンプはいじるな」「フィラー（えー、あー）を消して」といった具体的な指示が記述されています。
*   `temperature`: 生成のランダム性（デフォルト: `0.3`）。値を低くすることで、創作的な文章を作らせず、忠実な修正を行わせます。

### 中間生成ファイル
*   `data/output/{video_id}_refined.json`: 補正後のデータ。`text` フィールドが修正されており、`original_text` フィールドに元の認識結果が残されています。

### 動作確認・検証
*   **確認方法**: `_raw.json` と `_refined.json` を比較し、明らかな誤字が修正されているか、不要な「(music)」などの記述が削除されているかを確認します。

---

## 5. GPU・リソース管理 (共通基盤)

**目的**: 限られたハードウェアリソースで最大限のパフォーマンスを引き出す。

### 仕組み
`utils/gpu_manager.py` がハードウェアを抽象化します。
*   **自動デバイス検出**: NVIDIA GPU (`cuda`), Apple Silicon (`mps`), CPU を自動判定し、最適なデバイスを割り当てます。
*   **メモリ管理**: DemucsやWhisperはメモリを大量に消費するため、各ステージの終了時に明示的にVRAM上のモデルを破棄（Garbage Collection）し、次のステージのためにメモリを空ける処理を行っています。

### 動作確認
*   ログの冒頭に `System information` として検出されたデバイス（例: `cuda`, `mps`）が表示されていることを確認してください。